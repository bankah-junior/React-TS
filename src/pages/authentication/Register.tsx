import RegisterTemplate from '../../components/authentication/RegisterTemplate'

type Props = {}

const Register = (props: Props) => {
  return (
    <RegisterTemplate text='REGISTER' />
  )
}

export default Register